# 여러 AI 작업 방식 — 추가 조사·설계 검토

2026-09-25 · ChatGPT 웹 세션 · 기준 main `6e69346852e4863af9a160523170135bdcec7d03`.

**상태: 조사 중인 중간 저장. 채택된 운영 규칙이나 구현 완료 보고가 아니다.** 사용자가 첨부한 「여러 AI 작업 방식 정리와 도구 조사」(2026-09-25)를 검증·확장한다. 원문의 사용자 판단대로, 수퍼바이저·작업판 구조로 즉시 전환하지 않는다.

## 접근 범위와 보존

- GitHub 연결로 AGENTS.md, NEXT-SESSION.md, docs/COLLABORATION.md와 열린 PR·브랜치 목록을 직접 확인했다. 시작 시 열린 PR은 없고 main만 있었다.
- 사용자 PC/WSL과 설치된 CLI의 현재 동작은 이 세션에서 관측하지 않았다. 기존 로컬 관측은 그대로 보존한다.
- 웹 컨테이너에서 Git clone은 DNS 해석 실패로 실행되지 않았다. 저장소 읽기·쓰기와 검증 상태 확인은 GitHub 연결을 사용한다.
- 코드·실행 권한·원장·유료 API 설정·원본 앱 화면을 변경하지 않는다. 이 조사에서 참여자 모델을 실행하지 않는다.
- 본 브랜치에 작업 단위별로 즉시 원격 커밋한다. main 직접 수정·병합은 하지 않는다.

## 검토 질문

1. 개발을 맡는 수퍼바이저와 앱의 독립 판단 참여자는 어떻게 분리해야 하는가?
2. 공유 파일은 장기 상태 저장소이지 무제한 모델 문맥이 아니다. 무엇을 항상 읽고 무엇을 필요할 때 읽는가?
3. 자동 요약·재개·분기·새 세션 인계는 어떻게 다르며, 현재 공식 도구는 어디까지 지원하는가?
4. 작업판을 실제 자동 실행에 쓰려면 담당권·중복 방지·취소·재시도·호출 상한 중 무엇이 필요한가?
5. 기존 core/app 실행·원장·UI를 활용하면서 별도 프레임워크와 문서 중복을 늘리지 않을 방법은 무엇인가?
6. 다른 제품의 편의 기능 중 사용자의 중계·대기·검토 부담을 실제로 줄일 후보는 무엇인가?

## 다음에 이어갈 곳

- 첨부 원문의 핵심 주장과 최신 공식 문서/원본 저장소를 대조한다. 확인 불가와 반증은 구분한다.
- app/core 문서와 관련 구현을 확인해 이미 있는 기능과 새 기능을 구분한다.
- 결과 문서에는 원문 평가, 출처별 검증, 권장 구조, 컨텍스트 인계 절차, 우선순위·시범 완료 조건을 남긴다.
- 검토 목록과 NEXT-SESSION.md에 링크를 추가하고 PR 템플릿을 채운다. 최종 head의 GitHub Actions를 확인한다.
